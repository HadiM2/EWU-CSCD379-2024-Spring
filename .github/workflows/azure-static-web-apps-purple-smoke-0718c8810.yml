name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - Final
  workflow_dispatch:

jobs:
        build_and_deploy_job:
          runs-on: ubuntu-latest
          name: Build and Deploy Job
          steps:
            - name: Get Code
              uses: actions/checkout@v3
              with:
                submodules: true
                lfs: false
      
            - name: Setup Node.js environment
              uses: actions/setup-node@v4.0.2
              with: 
                node-version: 18
      
            - name: Install packages
              run: npm ci
              working-directory: ./Hillbiddle/CountryCrush/
      
            - name: Build App
              run: npm run generate
              working-directory: ./Hillbiddle/CountryCrush/
      
            - name: Vitest
              run: npm run test
              working-directory: ./Hillbiddle/CountryCrush/
      
              
            - name: Upload artifact
              uses: actions/upload-artifact@v2
              with:
                name: build-output
                path: ./Hillbiddle/CountryCrush/.output/public
                
            - name: Download artifact
              uses: actions/download-artifact@v2
              with:
                name: build-output
                path: ./Hillbiddle/CountryCrush/.output/public
    
            - name: Build And Deploy
              id: builddeploy
              uses: Azure/static-web-apps-deploy@v1
              with:
                azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PURPLE_SMOKE_0718C8810 }}
                repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
                action: "upload"
                ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
                # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
                app_location: "./Hillbiddle/CountryCrush/.output/public" # App source code path
                api_location: ".output/server" # Api source code path - optional
                output_location: ".output/public" # Built app content directory - optional
                ###### End of Repository/Build Configurations ######
